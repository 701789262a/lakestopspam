users:
  - username: admin
    role: admin
    # Genera hash con: npm run hash-password -- "admin-password"
    passwordHash: "$2b$12$replace_admin_hash"

  - username: node-1
    role: client
    node: node-1
    # Genera hash con: npm run hash-password -- "node-password"
    passwordHash: "$2b$12$replace_client_hash"
